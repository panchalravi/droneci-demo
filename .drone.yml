kind: pipeline
type: docker
name: default

steps:
- name: init
  image: alpine:latest
  commands:
  - echo "Initializing..."
  - ls -l
- name: build
  image: alpine:latest
  commands:
  - echo "Starting to build the sample project"
  - echo "Building project..."
